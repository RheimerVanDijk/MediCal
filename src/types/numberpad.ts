import { NativeMethods } from "react-native";

export type type_numberpad_numbers_row = number[];
export type type_numberpad_numbers_num = number;

export type type_numberpad_lastrow = {
  function: () => void;
  name: string;
  style: object[];
  type: string;
  text: string;
};
